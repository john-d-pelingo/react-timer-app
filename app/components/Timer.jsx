let React = require('react');

let Timer = React.createClass({
    render: function () {
        return (
            <div>
                <h1>Timer component</h1>
            </div>
        );
    }
});

module.exports = Timer;